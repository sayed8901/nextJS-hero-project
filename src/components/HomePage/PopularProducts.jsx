import wait from '@/utilities/wait';
import React from 'react';

const PopularProducts = async () => {
    await wait();

    return <div className='my-5'>PopularProducts</div>;
};

export default PopularProducts;